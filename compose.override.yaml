# Development environment override
services:
  video-incrustator:
      build:
          dockerfile: ./Dockerfile
          target: dev
          args:
              SERVICE_PORT: 5000
              SERVICE_NAME: video-incrustator
