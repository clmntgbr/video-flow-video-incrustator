# Production environment override
services:
  video-incrustator:
      build:
          dockerfile: ./Dockerfile
          target: prod
          args:
              SERVICE_PORT: 5000
              SERVICE_NAME: video-incrustator
